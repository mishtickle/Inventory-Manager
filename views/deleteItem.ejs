<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Are you sure you want to delete this item?</h1>
    <% console.log(locals) %>
    <%if (locals.item){%>
        <p><%=locals.item[0].item%></p>
        <p><%=locals.item[0].quantity%></p>
        <form action="/deleteitem?q=<%=locals.itemId%>" method="POST">
            <button>Yes Delete</button>
        </form>
        
    <%}%>
</body>
</html>